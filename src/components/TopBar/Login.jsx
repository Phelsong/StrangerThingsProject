import React from "react"

const Login = ()=>{

    return(
        <div id="login">
            Login
            <form id="userName">
            UserName
            </form>
            <form id="password">Password</form>
        </div>
    )}
export default Login